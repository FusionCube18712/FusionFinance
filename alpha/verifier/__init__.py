"""Fail-closed verification components for FusionFinance.

Import the required component from its focused module, for example
``alpha.verifier.evidence`` or ``alpha.verifier.policy``. The package root does
not eagerly import optional NumPy, scikit-learn, or PyTorch backends.
"""

__all__: tuple[str, ...] = ()
